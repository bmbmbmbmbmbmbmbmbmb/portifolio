const menuhaburguer = document.querySelector(".menu-haburguer");
const menu = document.querySelector(".menu");

menuhaburguer.addEventListener('click', () => {

    menu.classList.toggle('active');

});